Q. 얕은 복사와 깊은 복사

A.
얕은 복사는 객체의 멤버 값을 그대로 복사하는 방식입니다. 객체가 포인터 멤버를 가지고 있다면 포인터의 주소값만 복사되므로, 원본과 복사본의 포인터가 같은 자원을 가리키게 됩니다.

깊은 복사는 새로운 메모리를 할당한 뒤, 원본 포인터가 가리키는 데이터까지 복사하는 방식입니다. 따라서 원본과 복사본이 서로 독립적인 자원을 소유합니다.
