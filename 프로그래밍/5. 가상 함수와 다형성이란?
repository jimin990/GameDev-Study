Q. 가상 함수와 다형성은 무엇인가요?

A.
가상 함수를 호출하면 컴파일 시점의 포인터 타입이 아니라 런타임 시점의 실제 객체 타입을 기준으로 함수가 결정됩니다.
일반적으로 컴파일러는 가상 함수 테이블인 vtable과 이를 가르키는 vptr을 이용해 이를 구현합니다.

다형성이란 동일한 인터페이스를 사용하지만, 실제 객체의 타입에 따라 서로 다른 동작을 수행하는 성질입니다.

C++에서는 부모 클래스의 함수를 vitrual로 선언하고 자식 클래스에서 재정의함으로써 런타임 다형성을 구현할 수 있습니다.
